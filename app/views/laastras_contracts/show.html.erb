
<%= react_component('LaastrasContract', {
    title: @contract.title,
    description: @contract.description,
    contract_definition_nota_benne: (I18n.t 'contract_definition_nota_benne'),
    contract_nb_id: (@contract.id.to_s + '-nb'),
    contract_id: @contract.id.to_s,
    contract_title_id: (@contract.id.to_s + '-title')
})%>